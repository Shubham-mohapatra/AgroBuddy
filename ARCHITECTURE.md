#  System Architecture & Data Flow

## Complete System Overview

```
┌──────────────────────────────────────────────────────────────┐
│                     MOBILE APPLICATION                        │
│  ┌────────────────────────────────────────────────────────┐  │
│  │            React Native (Expo) Frontend                │  │
│  │                                                        │  │
│  │   Camera/Gallery  →  Upload Image     →        Results │  │
│  │                                                        │  │
│  │  Redux Store: Scans, Saved Diagnoses, Stats          │  │
│  └────────────────┬───────────────────────────────────────┘  │
└───────────────────┼───────────────────────────────────────────┘
                    │
                    │ HTTP POST /api/disease/detect
                    │ (multipart/form-data with image)
                    ▼
┌──────────────────────────────────────────────────────────────┐
│                    BACKEND API SERVER                         │
│  ┌────────────────────────────────────────────────────────┐  │
│  │           Node.js Express Backend (Port 3000)          │  │
│  │                                                        │  │
│  │  1️⃣ Receive image upload (Multer)                    │  │
│  │  2️⃣ Optimize image (Sharp) → 224x224                 │  │
│  │  3️⃣ Forward to ML Service ──────────┐                │  │
│  │  4️⃣ Get ML prediction               │                │  │
│  │  5️⃣ Lookup disease database         │                │  │
│  │  6️⃣ Format response with solutions  │                │  │
│  │                                      │                │  │
│  │  Disease Database:                   │                │  │
│  │  - Symptoms                          │                │  │
│  │  - Causes                            │                │  │
│  │  - Solutions                         │                │  │
│  │  - Prevention                        │                │  │
│  │  - Treatment Products                │                │  │
│  └──────────────────────────────────────┼────────────────┘  │
└────────────────────────────────────────┼────────────────────┘
                                         │
                                         │ HTTP POST /predict
                                         │ (form-data with image)
                                         ▼
┌──────────────────────────────────────────────────────────────┐
│                   ML SERVICE SERVER                           │
│  ┌────────────────────────────────────────────────────────┐  │
│  │        Python Flask ML Service (Port 5000)             │  │
│  │                                                        │  │
│  │  1 Receive image                                     │  │
│  │  2 Preprocess image                                  │  │
│  │     - Convert to RGB                                  │  │
│  │     - Resize to 224x224                               │  │
│  │     - Normalize (0-1)                                 │  │
│  │     - Add batch dimension                             │  │
│  │                                                        │  │
│  │  3 Load TensorFlow/Keras model                      │  │
│  │                                                        │  │
│  │  4 Run inference                                     │  │
│  │     - Get predictions for 38 classes                  │  │
│  │     - Extract top 3 predictions                       │  │
│  │     - Format class names                              │  │
│  │                                                        │  │
│  │  5 Return JSON response                              │  │
│  │     {                                                 │  │
│  │       "diseaseId": "tomato___early_blight",          │  │
│  │       "confidence": 0.95,                            │  │
│  │       "plant": "Tomato",                             │  │
│  │       "disease": "Early blight"                      │  │
│  │     }                                                 │  │
│  └────────────────────────┬───────────────────────────────┘  │
└───────────────────────────┼───────────────────────────────────┘
                            │
                            ▼
              ┌──────────────────────────┐
              │   TensorFlow/Keras Model  │
              │   plant_disease_model.h5  │
              │                          │
              │   Input: (224, 224, 3)   │
              │   Output: 38 classes     │
              │                          │
              │   PlantVillage Dataset   │
              └──────────────────────────┘
```

## Data Flow Example

### User Takes Photo → Gets Results

```
Step 1: Image Capture
┌────────────────────┐
│      User Phone    │
│                    │
│  [Take Photo]      │
│  ↓                 │
│  plant_leaf.jpg    │
│  (high resolution) │
└─────────┬──────────┘
          │
          ▼

Step 2: Upload to Backend
┌────────────────────────────────────┐
│  POST /api/disease/detect          │
│  Content-Type: multipart/form-data │
│  Body: { image: <file> }           │
└─────────┬──────────────────────────┘
          │
          ▼

Step 3: Backend Processing
┌────────────────────────────────────┐
│  Node.js Backend                   │
│  1. Save: plant-123456.jpg         │
│  2. Optimize: 224x224, JPEG        │
│  3. Create FormData                │
│  4. POST to ML Service             │
└─────────┬──────────────────────────┘
          │
          ▼

Step 4: ML Service Prediction
┌────────────────────────────────────┐
│  Python Flask                      │
│  1. Receive image                  │
│  2. Preprocess                     │
│     ├─ Resize (224, 224)          │
│     ├─ Normalize /255             │
│     └─ np.expand_dims             │
│  3. model.predict(image)           │
│  4. Get top predictions            │
│     ├─ Class 29: 0.95 confidence  │
│     ├─ Class 30: 0.03 confidence  │
│     └─ Class 31: 0.01 confidence  │
│  5. Format response                │
└─────────┬──────────────────────────┘
          │
          ▼

Step 5: ML Response
┌────────────────────────────────────┐
│  {                                 │
│    "success": true,                │
│    "diseaseId": "tomato___...",    │
│    "confidence": 0.95,             │
│    "plant": "Tomato",              │
│    "disease": "Early blight",      │
│    "predictions": [...]            │
│  }                                 │
└─────────┬──────────────────────────┘
          │
          ▼

Step 6: Backend Enhancement
┌────────────────────────────────────┐
│  Node.js Backend                   │
│  1. Get ML prediction              │
│  2. Lookup disease database        │
│     ├─ Symptoms                    │
│     ├─ Causes                      │
│     ├─ Solutions (7 items)         │
│     ├─ Prevention (5 items)        │
│     └─ Treatment Products          │
│  3. Format full response           │
│  4. Clean up temp files            │
└─────────┬──────────────────────────┘
          │
          ▼

Step 7: Final Response to App
┌────────────────────────────────────┐
│  {                                 │
│    "success": true,                │
│    "prediction": {                 │
│      "disease": "Early Blight",    │
│      "plant": "Tomato",            │
│      "confidence": 95,             │
│      "severity": "Medium"          │
│    },                              │
│    "details": {                    │
│      "description": "...",         │
│      "symptoms": [...],            │
│      "causes": [...]               │
│    },                              │
│    "solutions": [...],             │
│    "prevention": [...],            │
│    "treatmentProducts": [...]      │
│  }                                 │
└─────────┬──────────────────────────┘
          │
          ▼

Step 8: Display in App
┌────────────────────────────────────┐
│     User Phone                     │
│                                    │
│  ┌──────────────────────────────┐ │
│  │ 🌿 Tomato Early Blight       │ │
│  │ 95% Confidence               │ │
│  │                              │ │
│  │ Description: ...             │ │
│  │                              │ │
│  │ Symptoms:                    │ │
│  │ ✓ Dark brown spots...        │ │
│  │ ✓ Yellowing around...        │ │
│  │                              │ │
│  │ Solutions:                   │ │
│  │ 1. Remove infected leaves    │ │
│  │ 2. Apply copper fungicide    │ │
│  │ 3. Improve air circulation   │ │
│  │                              │ │
│  │ [Save Diagnosis] [Share]     │ │
│  └──────────────────────────────┘ │
└────────────────────────────────────┘
```

## Fallback Mechanism

```
┌────────────────────────┐
│  Backend tries ML      │
│  Service                │
└───────┬────────────────┘
        │
        ├─ Success ──────────┐
        │                    │
        └─ Failed ───┐       │
                     ▼       ▼
          ┌──────────────────────────┐
          │  Use Mock Predictions    │
          │  (Fallback for testing)  │
          └──────────────────────────┘
                     │
                     ▼
          ┌──────────────────────────┐
          │  App still works!        │
          │  Shows sample results    │
          └──────────────────────────┘
```

## Request/Response Timing

```
Typical Flow Timeline:
─────────────────────────────────────────────

0ms    │ User takes photo
       │
100ms  │ Upload starts to backend
       │
150ms  │ Backend receives, processes image
       │
200ms  │ Backend sends to ML Service
       │
1500ms │ ML Service loads & preprocesses
       │ TensorFlow runs inference
       │
1700ms │ ML Service returns prediction
       │
1750ms │ Backend enriches with database info
       │
1800ms │ Response sent to frontend
       │
1900ms │ App displays results to user

Total: ~1.8 seconds from click to result!
```

## Error Handling Flow

```
┌─────────────────┐
│  Image Upload   │
└────────┬────────┘
         │
         ├─ No file? ──→ Error: "No image provided"
         │
         ├─ Too large? ─→ Error: "File size >10MB"
         │
         ├─ Wrong type? → Error: "Only JPEG/PNG"
         │
         ▼
┌─────────────────┐
│  Backend API    │
└────────┬────────┘
         │
         ├─ ML Service down? ──→ Use Mock Data
         │
         ├─ Timeout? ──────────→ Retry → Mock Data
         │
         ▼
┌─────────────────┐
│  ML Service     │
└────────┬────────┘
         │
         ├─ Model not loaded? ─→ Error: "Model unavailable"
         │
         ├─ Invalid image? ────→ Error: "Cannot process"
         │
         ▼
┌─────────────────┐
│  Success!       │
│  Return Results │
└─────────────────┘
```

## Ports & URLs Reference

```
Development Environment:

┌──────────────┬──────┬───────────────────────────┐
│   Service    │ Port │          URL              │
├──────────────┼──────┼───────────────────────────┤
│ Frontend     │ 8081 │ exp://localhost:8081      │
│ Backend API  │ 3000 │ http://localhost:3000     │
│ ML Service   │ 5000 │ http://localhost:5000     │
└──────────────┴──────┴───────────────────────────┘

API Endpoints:

Backend:
  ✓ GET  /health
  ✓ POST /api/disease/detect
  ✓ GET  /api/disease/:diseaseId
  ✓ GET  /api/disease/:diseaseId/solutions
  ✓ GET  /api/disease/plants/list

ML Service:
  ✓ GET  /health
  ✓ POST /predict
  ✓ GET  /classes
```

## Technology Stack Map

```
Frontend (React Native)
├── expo ~54.0.0
├── react-native
├── @reduxjs/toolkit
├── expo-camera
├── expo-image-picker
└── react-native-safe-area-context

Backend (Node.js)
├── express ^4.18.2
├── multer ^1.4.5
├── sharp ^0.33.0
├── axios ^1.6.0
├── cors ^2.8.5
└── dotenv ^16.3.1

ML Service (Python)
├── Flask 3.0.0
├── tensorflow 2.15.0
├── Pillow 10.1.0
├── numpy 1.24.3
├── flask-cors 4.0.0
└── gunicorn 21.2.0 (production)
```

This architecture provides:
-  Separation of concerns
-  Scalability
-  Easy maintenance
-  Fallback mechanisms
-  Production-ready setup
